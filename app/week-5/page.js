import ProductList from "./new-item"

export default function Page(){
    return (
        <main style={{display: 'flex', flexDirection: 'column', alignItems: "center", justifyContent: "center"}}>
            <ProductList/>
        </main>
    )
}